package ControlFlow;

public class D_TernaryOperator {
    public static void main(String[] args) {
        int income = 120_000;
        String className = income > 100_000 ? "First" : "Economy";
        // if the income is over 100,000 then the string will be defined as "First" if not then "Economy"
    }
}
